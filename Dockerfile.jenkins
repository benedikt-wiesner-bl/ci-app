FROM jenkins/jenkins:lts

USER root

# Install Docker CLI, Git und SSH
RUN apt-get update && \
    apt-get install -y \
      docker.io \
      git \
      openssh-client \
      docker-compose && \
    rm -rf /var/lib/apt/lists/*

USER jenkins
